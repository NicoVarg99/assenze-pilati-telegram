# assenze-pilati-telegram

Bot creato per l'ITET Pilati - https://www.istitutopilati.it

Specificare il token di accesso al proprio bot nella variabile d'ambliente "TOKEN".

```bash
# installa le dipendenze
npm install

# avvia il bot
npm start
```
